name: kennzeichen-fleet-gate

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  fleet-gate:
    uses: dalexsy/balcony-deploy-control/.github/workflows/reusable-fleet-gate.yml@main
    with:
      app-name: kennzeichen
      app-repo: dalexsy/kennzeichen
